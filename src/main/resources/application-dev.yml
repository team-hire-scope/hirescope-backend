spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/hirescope
    username: postgres
    password: hmjeoung33
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  data:
    redis:
      host: localhost
      port: 6379

ai-server:
  url: http://localhost:8000
  timeout: 300000  # 5분

logging:
  level:
    com.project.hirescopebackend: DEBUG
    org.hibernate.SQL: DEBUG
